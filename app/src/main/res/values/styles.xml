<resources>

    <style name="SplashScreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/transparent</item>
        <item name="android:windowBackground">@drawable/splash_screen_new</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/transparent</item>
        <item name="android:windowBackground">@drawable/background_small</item>
    </style>

    <style name="BlankScreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/background_letters_generator</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar.SettingActivity"  parent="AppTheme.NoActionBar">
        <item name="android:textColorSecondary">@color/strongOrange</item>
        <item name="android:textColorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimary">#FF4081</item>
      <!--when the check box is checked-->
       <item name="colorControlNormal">@color/strongOrange</item>
      <!--when the check box is unchecked-->
       <item name="colorControlActivated">@color/transparent</item>
 </style>

</resources>
